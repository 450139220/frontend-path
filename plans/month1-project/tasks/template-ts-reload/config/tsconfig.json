{
  "compilerOptions": {
    "target": "es6",                    // 编译成 ES6 版本的 JavaScript
    "module": "esnext",                // 适用于浏览器
    // "module": "commonjs"                // 适用于Nodejs
    "strict": true,                      // 启用所有严格类型检查
    "esModuleInterop": true,             // 使 CommonJS 和 ES6 模块之间互相兼容
    "skipLibCheck": true,                // 跳过库的类型检查，提高编译速度
    "forceConsistentCasingInFileNames": true, // 强制文件名的大小写一致性
    "outDir": "./dist",                  // 编译后的文件输出目录
    "rootDir": "./src",                  // 项目源代码所在的目录
    "resolveJsonModule": true,           // 支持导入 `.json` 文件
    "isolatedModules": true,             // 启用隔离的模块编译（适用于一些特定的构建工具）
    "noImplicitAny": true,               // 禁止隐式 `any` 类型
    "moduleResolution": "node"           // 使用 Node.js 模块解析策略
  },
  "include": [
    "src/**/*.ts",                           // 包含的源文件目录
    "src/**/*.html",
  ],
  "exclude": [
    "node_modules",                      // 排除 `node_modules` 文件夹
    "dist"                               // 排除编译后的文件夹
  ]
}
